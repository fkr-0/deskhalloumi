schema_version: 2
updated_at: '2026-08-02'
project:
  name: DeskHalloumi
  coordinated_workspace_version: 0.4.0
  historical_ledger: docs/history/tasks-through-0.3.2.yml
principles:
- Preserve every released CLI, configuration, IPC, path, application-ID, service, generated-i3, and compatibility contract
  unless a future release documents a migration.
- Add behavior through the existing runtime, provider, menu, action, and hotkey contracts rather than parallel stacks.
- Require focused regression evidence and full local release gates before tagging.
active:
- id: STABLE-100
  target: 1.0.0
  title: Stable desktop-control contract
  status: planned
  work:
  - Inventory and classify all public compatibility surfaces.
  - Thin the remaining menu/action Iced composition root and stabilize only intentional Rust/plugin APIs.
  - Complete package-quality install, migration, upgrade, rollback, provenance, and removal evidence.
  - Decide the 1.x policy for the unilii compatibility layer.
completed:
- id: PROVIDER-040
  target: 0.4.0
  title: Provider lifecycle completion release candidate
  status: complete
  implementation_commit: 50ed55c
  completed:
  - All production providers use shared bounded lifecycle, timeout, cancellation, generation, status, and shutdown helpers.
  - Built-in audio, network, and system ownership moved from main.rs to provider_runtime.rs.
  - Executable fixture backends cover Clock, Battery, Tmux, Audio, Network, and System.
  - Provider health, generations, age, refresh policy, and bounded errors are visible over the action bus and panel.
  - Provider refresh/shutdown failure and timeout counters are live runtime metrics.
  - A 128-generation replacement regression proves stale status and refresh permits do not leak.
  - Native AArch64 archive validation gates multi-architecture publication.
  - ADR 0001 rejects a musl artifact for 0.4.x.
  local_evidence:
  - cargo fmt --all -- --check
  - scripts/test_safe.sh
  - scripts/test_i3_hotkeys.sh
  - CARGO_INCREMENTAL=0 cargo clippy --workspace --all-targets -- -D warnings
  - CARGO_INCREMENTAL=0 cargo check --workspace --all-features
  - independent OpenCode review 6f05520c-4721-456e-b6c4-5f2e26d31fc1 recommended local release with no blockers
  remote_condition:
  - The native ubuntu-24.04-arm workflow must execute successfully before GitHub Release publication.
  tag: v0.4.0
  revision: 373ce47911869fa63b847433bd9f0272f46ada4f
- id: PATCH-033
  target: 0.3.3
  status: complete
  tag: v0.3.3
  revision: c2daa0a1a09cb1b739c2a588945d11efdb9f10f4
  evidence:
  - Deterministic x86-64 archive and checksum.
  - Source-tree-independent installation, command smoke, runtime contract, and removal verification.
  - Exact IPC boundary and durable-write failure-phase tests.
  - Executable Clock, Battery, and Tmux fixture sources.
  - Historical task ledger archived and tracked backup sources removed.
